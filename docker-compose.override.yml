# Docker Compose override file for local development customization
# This file allows developers to customize their local environment
# without modifying the main docker-compose.dev.yml file

version: '3.8'

services:
  # Example customizations - uncomment and modify as needed
  
  # Custom backend configuration
  # backend:
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=local
  #     - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/golfdb_local
  #     - JAVA_OPTS=-Xmx1024m -Xms512m -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005
  #   ports:
  #     - "5005:5005"  # Debug port
  #   volumes:
  #     - ~/.m2:/root/.m2:cached  # Mount local Maven cache
  
  # Custom frontend configuration
  # frontend:
  #   environment:
  #     - VITE_API_URL=http://localhost:8080
  #     - VITE_DEBUG=true
  #   volumes:
  #     - ./frontend/src:/app/src:cached
  #     - ./frontend/public:/app/public:cached
  
  # Custom database configuration
  # db:
  #   environment:
  #     - POSTGRES_DB=golfdb_local
  #     - POSTGRES_USER=localuser
  #     - POSTGRES_PASSWORD=localpass
  #   ports:
  #     - "5433:5432"  # Use different port to avoid conflicts
  
  # Additional development services
  # pgadmin:
  #   image: dpage/pgadmin4:latest
  #   environment:
  #     - PGADMIN_DEFAULT_EMAIL=admin@example.com
  #     - PGADMIN_DEFAULT_PASSWORD=admin
  #   ports:
  #     - "5050:80"
  #   depends_on:
  #     - db
  #   networks:
  #     - golf-dev-network
  
  # Redis Commander for Redis management
  # redis-commander:
  #   image: rediscommander/redis-commander:latest
  #   environment:
  #     - REDIS_HOSTS=local:redis:6379
  #   ports:
  #     - "8081:8081"
  #   depends_on:
  #     - redis
  #   networks:
  #     - golf-dev-network

# Custom volumes for local development
# volumes:
#   local_pgdata:
#     driver: local
#   local_node_modules:
#     driver: local

# Custom networks for local development
# networks:
#   golf-local-network:
#     driver: bridge